// Package alertlog provides an in-memory ring buffer for recording
// drift alert events. It supports a configurable capacity and exposes
// recent entries for use by the health check and diagnostic endpoints.
package alertlog
